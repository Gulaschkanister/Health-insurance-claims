# AGENTS

## Projektstruktur

- Hauptprojekt: `GKVTransmitter/`
- UI: JavaFX + BootstrapFX
- Build: Maven

## Lokale Validierung

```bash
cd GKVTransmitter
mvn pmd:check
mvn test
mvn package -DskipTests
```
